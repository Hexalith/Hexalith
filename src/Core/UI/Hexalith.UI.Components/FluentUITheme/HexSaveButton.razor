@inherits FluentButton
<FluentButton Action="@Action"
              AdditionalAttributes="AdditionalAttributes"
              Appearance="@Appearance"
              Autofocus="@Autofocus"
              BackgroundColor="@BackgroundColor"
              Class="@Class"
              Color="@Color"
              CurrentValue="@CurrentValue"
              Data="@Data"
              Disabled="@Disabled"
              Enctype="@Enctype"
              FormId="@FormId"
              IconEnd="@IconEnd"
              IconStart="@(IconStart is null && IconEnd is null ? new Icons.Regular.Size16.Save() : IconStart)"
              Id="@Id"
              Loading="@Loading"
              Method="@Method"
              Name="@Name"
              NoValidate="@NoValidate"
              OnClick="@OnClick"
              ParentReference="@ParentReference"
              Required="@Required"
              StopPropagation="@StopPropagation"
              Style="@Style"
              Target="@Target"
              Title="@Title"
              Type="@Type"
              Value="@Value">
    <ChildContent>
        @if (ChildContent is null)
        {
           @Labels.Save
        }
        else
        {
            @ChildContent
        }
    </ChildContent>
</FluentButton>

@code {
}
