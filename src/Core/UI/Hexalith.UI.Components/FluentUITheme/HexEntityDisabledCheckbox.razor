@inherits FluentCheckbox

<FluentCheckbox AdditionalAttributes="AdditionalAttributes"
                AriaLabel="@AriaLabel"
                Autofocus="@Autofocus"
                CheckState="@CheckState"
                CheckStateChanged="@CheckStateChanged"
                Class="@Class"
                Data="@Data"
                Disabled="@Disabled"
                DisplayName="@DisplayName"
                Embedded="@Embedded"
                Field="@Field"
                Id="@Id"
                Immediate="@Immediate"
                ImmediateDelay="@ImmediateDelay"
                Label="@(Label ?? Labels.Disabled)"
                Name="@Name"
                ParentReference="@ParentReference"
                Placeholder="@Placeholder"
                ReadOnly="@ReadOnly"
                Required="true"
                ShowIndeterminate="@ShowIndeterminate"
                Style="@(Style ?? "width:100%;")"
                ThreeState="@ThreeState"
                ThreeStateOrderUncheckToIntermediate="@ThreeStateOrderUncheckToIntermediate"
                Value="@Value"
                ValueChanged="@ValueChanged"
                ValueExpression="@ValueExpression">
    <LabelTemplate>
        @LabelTemplate
    </LabelTemplate>
    <ChildContent>
        @ChildContent
    </ChildContent>
</FluentCheckbox>
@code {

}
