@inherits FluentTextField

<FluentTextField AdditionalAttributes="AdditionalAttributes"
                 Appearance="@Appearance"
                 AriaLabel="@AriaLabel"
                 AutoComplete="@AutoComplete"
                 Autofocus="@Autofocus"
                 Class="@Class"
                 Data="@Data"
                 DataList="@DataList"
                 Disabled="@Disabled"
                 DisplayName="@DisplayName"
                 Embedded="@Embedded"
                 Field="@Field"
                 Id="@Id"
                 Immediate="@Immediate"
                 ImmediateDelay="@ImmediateDelay"
                 InputMode="@InputMode"
                 Label="@(Label ?? Labels.Name)"
                 Maxlength="@Maxlength"
                 Minlength="@Minlength"
                 Name="@Name"
                 ParentReference="@ParentReference"
                 Placeholder="@Placeholder"
                 Pattern="@Pattern"
                 ReadOnly="@ReadOnly"
                 Required="true"
                 Size="@Size"
                 Spellcheck="@Spellcheck"
                 Style="@(Style ?? "width:100%;")"
                 TextFieldType="@TextFieldType"
                 Value="@Value"
                 ValueChanged="@ValueChanged"
                 ValueExpression="@ValueExpression">
    <LabelTemplate>
        @LabelTemplate
    </LabelTemplate>
    <ChildContent>
        @ChildContent
    </ChildContent>
</FluentTextField>
@code {

}
