@inherits FluentTextArea

<FluentTextArea AdditionalAttributes="AdditionalAttributes"
                 Appearance="@Appearance"
                 AriaLabel="@AriaLabel"
                 Autofocus="@Autofocus"
                 Class="@Class"
                 Cols="@Cols"
                 ChildContent="@(ChildContent!)"
                 Data="@Data"
                 DataList="@DataList"
                 Disabled="@Disabled"
                 DisplayName="@DisplayName"
                 Embedded="@Embedded"
                 Field="@Field"
                 Form="@Form"
                 Id="@Id"
                 Immediate="@Immediate"
                 ImmediateDelay="@ImmediateDelay"
                 Label="@(Label ?? Labels.Comments)"
                 LabelTemplate="@(LabelTemplate!)"
                 Maxlength="@Maxlength"
                 Minlength="@Minlength"
                 Name="@Name"
                 ParentReference="@ParentReference"
                 Placeholder="@Placeholder"
                 ReadOnly="@ReadOnly"
                 Required="false"
                 Resize="@Resize"
                 Rows="@(Rows ?? 3)"
                 Spellcheck="@Spellcheck"
                 Style="@(Style ?? "width:100%;")"
                 Value="@Value"
                 ValueChanged="@ValueChanged"
                 ValueExpression="@ValueExpression">
</FluentTextArea>
@code {

}
